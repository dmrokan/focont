---

name: Focont build

on:
  push:
    tags:
      - "v*"

jobs:
  call-reusable-workflow:
    uses: ./.github/workflows/build.yml
    secrets: inherit
    with:
      pypi-url: https://pypi.org/p/focont